{% extends 'base.html.twig' %}

{% block title %}Edit Bill{% endblock %}

{% block body %}
    <h1>Edit Bill</h1>

    <p>{{ include('bill/_form.html.twig', {'button_label': 'Update'}) }}<p>

    <p><a href="{{ path('bill_index') }}">Back to list</a><p>

    {{ include('bill/_delete_form.html.twig') }}
{% endblock %}
