{% extends 'base.html.twig' %}

{% block title %}Edit Food{% endblock %}

{% block body %}
    <h1>Edit Food</h1>

    <h3>{{ include('food/form.html.twig', {'button_label': 'Update'}) }}</h3>

    <h3><a href="{{ path('food_index') }}">back to list</a></h3>

    <h3>{{ include('food/delete_form.html.twig') }}</h3>
{% endblock %}
