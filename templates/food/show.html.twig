{% extends 'base.html.twig' %}

{% block title %}Food{% endblock %}

{% block body %}
    <h1>Food</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ food.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ food.Name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ food.Category }}</td>
            </tr>
            <tr>
                <th>UnitPrice</th>
                <td>{{ food.UnitPrice }}</td>
            </tr>
            <tr>
                <th>Chef</th>
                <td>{{ food.chef.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('food_index') }}">back to list</a>

    <a href="{{ path('food_edit', {'id': food.id}) }}">edit</a>

    {{ include('food/delete_form.html.twig') }}
{% endblock %}
